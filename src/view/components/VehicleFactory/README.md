# VehicleFactory Component

Компонент фабрики, которая производит технику (автомобили и самолеты).

## Возможности

- 🚗 Производство автомобилей
- ✈️ Производство самолетов
- Анимация выезда техники из фабрики
- Событие при завершении производства
- Блокировка кнопок во время производства

## Использование

### 1. Добавьте контейнер в HTML

```html
<div id="factory-container"></div>
```

### 2. Инициализируйте компонент

```typescript
import { VehicleFactory } from "./view/components/VehicleFactory/VehicleFactory";

// Монтируем компонент
const factory = VehicleFactory.mount("factory-container");

// Подписываемся на событие производства
factory.onVehicleProduced((vehicleType) => {
    console.log(`Vehicle produced: ${vehicleType}`);
    // Здесь можно добавить логику обработки произведенной техники
});

// Настраиваем обработчики кнопок
factory.setProduceCarClick(() => {
    factory.produceVehicle("car");
});

factory.setProducePlaneClick(() => {
    factory.produceVehicle("plane");
});
```

### 3. Программное управление

Можно вызывать производство техники программно:

```typescript
// Произвести автомобиль
factory.produceVehicle("car");

// Произвести самолет
factory.produceVehicle("plane");
```

## API

### Methods

#### `mount(id: string)`

Монтирует компонент в элемент с указанным id.

#### `produceVehicle(vehicleType: 'car' | 'plane')`

Запускает производство техники указанного типа. После завершания анимации вызывается callback из `onVehicleProduced`.

#### `onVehicleProduced(callback: (vehicleType: VehicleType) => void)`

Устанавливает callback, который будет вызван после завершения производства техники.

#### `setProduceCarClick(callback: () => void)`

Устанавливает обработчик клика на кнопку производства автомобиля.

#### `setProducePlaneClick(callback: () => void)`

Устанавливает обработчик клика на кнопку производства самолета.

## Визуальное оформление

- Фабрика отображается как здание с крышей
- Дорога с разметкой для движения техники
- Техника выезжает справа налево и останавливается на 30% от правого края
- Анимация длится 2 секунды

## Особенности

- Кнопки блокируются во время производства техники
- Техника отображается эмодзи (🚗 для автомобиля, ✈️ для самолета)
- Плавная анимация с помощью CSS transitions
