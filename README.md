# test-factory

Игра-симулятор фабрики по производству и ремонту техники, построенная на TypeScript с использованием Vite.

## 🎯 Ключевые архитектурные особенности

### Функциональный подход

Проект полностью построен на **функциональной парадигме программирования**:

- Отсутствие классов — используются только чистые функции и функции высшего порядка
- Иммутабельность данных где это возможно
- Композиция функций вместо наследования
- Event-driven архитектура на основе паттерна pub/sub (mitt)

### Полная независимость View и Model

**View** и **Model** разработаны **полностью независимо друг от друга**, что обеспечивает:

- ✅ **Переиспользуемость**: каждый слой может использоваться в других проектах без изменений
- ✅ **Тестируемость**: модель и представление можно тестировать изолированно
- ✅ **Масштабируемость**: легко заменить UI или бизнес-логику

**Model** (`RepairVehicleGameModel`) — чистая функция, которая:

- Не знает ничего о DOM и представлении
- Принимает конфигурацию и возвращает API с actions и events
- Может работать в любом окружении (браузер, Node.js, тесты)

**View** (компоненты в `src/view/components/`) — чистые функции, которые:

- Не содержат бизнес-логики
- Работают только с DOM и событиями пользователя
- Могут быть подключены к любой модели через callbacks

### Типобезопасная расширяемость через Generics

Проект активно использует **TypeScript Generics** для динамического расширения типов на основе `config.json`:

```typescript
RepairVehicleGameModel<Store, VehicleConfiguration>;
```

**Что это дает:**

- 🔧 **Конфигурируемость**: добавьте новый тип техники или компонент в `config.json` — TypeScript автоматически выведет правильные типы
- 🛡️ **Типобезопасность**: все операции проверяются на этапе компиляции
- 🚀 **Масштабируемость**: легко расширяется от 2 типов техники до любого количества

**Пример**: В `config.json` можно добавить новый тип техники (например, "Boat") с набором компонентов — и система автоматически будет работать с ним без изменения кода модели.

## 🏗️ Структура проекта

```
test-factory/
├── src/
│   ├── main.ts                    # Точка входа, связывание Model и View
│   ├── config.json                # ⚙️ Конфигурация техники и компонентов
│   ├── ConfigService.ts           # Сервис для работы с конфигурацией
│   │
│   ├── model/                     # 🧠 Бизнес-логика (независимый слой)
│   │   └── RepairVehicleGameModel.ts  # Модель игры (generic функция)
│   │
│   ├── view/                      # 🎨 Представление (независимый слой)
│   │   ├── common/
│   │   │   └── createViewComponent.ts  # Фабрика для создания компонентов
│   │   └── components/
│   │       ├── VehicleFactory/    # Компонент фабрики производства
│   │       ├── RepairStation/     # Компонент станции ремонта
│   │       └── Store/             # Компонент склада компонентов
│   │
│   └── style.css                  # Глобальные стили
│
├── index.html                     # HTML шаблон
├── vite.config.ts                 # Конфигурация Vite
├── tsconfig.json                  # Конфигурация TypeScript
└── package.json                   # Зависимости проекта
```

## 📦 Установка

```bash
pnpm install
```

## 🚀 Разработка

Запуск dev-сервера:

```bash
pnpm dev
```

## 🔨 Сборка

Сборка проекта для production:

```bash
pnpm build
```

Результат сборки будет находиться в папке `dist`.

## 👀 Предпросмотр

Предпросмотр production сборки:

```bash
pnpm preview
```

## 🎮 Описание игры

Симулятор фабрики, где игрок:

1. **Производит технику** (автомобили, самолеты) из компонентов со склада
2. **Проверяет технику** на повреждения
3. **Ремонтирует технику**, используя компоненты со склада

Все типы техники и их компоненты настраиваются через `config.json`.

## 🔧 Расширение функциональности

Чтобы добавить новый тип техники:

1. Откройте `src/config.json`
2. Добавьте новый тип в `vehicleConfiguration` с нужными компонентами
3. При необходимости добавьте новые компоненты в `storeConfiguration`

Готово! TypeScript автоматически выведет типы, а модель будет работать с новым типом техники.

---

_Исходя из изначальной задачи, напрашивается использование ECS-архитектуры (Entity Component System). Однако это не было реализовано, поскольку по условиям задачи неясно, будет ли в каждый момент времени много единиц техники, фабрик и ремонтных депо. Итерация по массивам с единственным элементом в системах выглядела бы избыточной. Тем не менее, для дальнейшего масштабирования проекта, если игровой мир разрастется и появится множество одновременно существующих сущностей, переход на ECS-архитектуру был бы оправданным решением._

_AI-ассистированная разработка (vibe coding) использовалась для написания конкретных визуальных компонентов, документирования кода и составления README. При этом архитектура модели, фабрика визуальных компонентов (createViewComponent) и логика связывания модели с представлением были спроектированы и написаны вручную._
